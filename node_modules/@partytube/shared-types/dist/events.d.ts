import { QueueItem, RoomMember } from './models';
export interface JoinRoomPayload {
    roomCode: string;
    nickname: string;
    role: 'host' | 'guest';
    hostToken?: string;
}
export interface AddQueueItemPayload {
    roomCode: string;
    videoId: string;
    title: string;
    thumbnailUrl: string;
    channelTitle: string;
    durationSeconds: number;
    addedBy: string;
}
export interface RemoveQueueItemPayload {
    roomCode: string;
    queueItemId: string;
    hostToken?: string;
    nickname?: string;
}
export interface ReorderQueuePayload {
    roomCode: string;
    sourceIndex: number;
    destinationIndex: number;
    hostToken: string;
}
export interface PlayerStateChangePayload {
    roomCode: string;
    state: 'playing' | 'paused' | 'ended';
    currentTime: number;
    hostToken?: string;
}
export interface PlayerSkipPayload {
    roomCode: string;
    hostToken?: string;
}
export interface PlayerSeekPayload {
    roomCode: string;
    time: number;
    by?: string;
    hostToken?: string;
}
export interface PlayerTimeUpdatePayload {
    roomCode: string;
    currentTime: number;
    duration: number;
}
export interface NowPlayingPayload {
    track: QueueItem | null;
    startedAt: number;
    isPlaying: boolean;
}
export interface ErrorMessagePayload {
    code: string;
    message: string;
}
export interface GuestJoinedPayload {
    nickname: string;
    guestCount: number;
    members?: RoomMember[];
}
export interface GuestLeftPayload {
    nickname: string;
    guestCount: number;
    members?: RoomMember[];
}
//# sourceMappingURL=events.d.ts.map